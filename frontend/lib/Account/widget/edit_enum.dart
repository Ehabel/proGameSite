enum EditProfileType {
  editMode,
  normalMode,
}
class EditProfile {
  static const EditProfileType editMode = EditProfileType.editMode;
  static const EditProfileType normalMode = EditProfileType.normalMode;
}
